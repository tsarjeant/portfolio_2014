$(document).ready(function(){

  $('.tt-scroller').slick({
    arrows:false,
    // accessibility: true,
    // adaptiveHeight: true,
    // cssEase: 'ease',
    // dots: true,
    // draggable: true,
    // easing: 'linear',
    // respondTo: 'window',
    // swipe: true,
    // touchMove: true,
    // useCSS: true,
    // variableWidth: true,
  });

});