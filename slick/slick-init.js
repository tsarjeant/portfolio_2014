$(document).ready(function(){
$('.tt-scroller').slick({
   // autoplay: true,
   // autoplaySpeed: 2500,
    infinite: true,
    arrows:true,
    cssEase:'ease',
    dots: true,
    slide: 'div',
    swipe: true,
  });
});